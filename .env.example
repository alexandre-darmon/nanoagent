# Create a key at https://openrouter.ai/keys
# The default model in llm.py is paid but costs about $0.0001 per run.
# OpenRouter also lists free models (ids ending in :free) — see the README on why they vary.
OPENROUTER_API_KEY=
